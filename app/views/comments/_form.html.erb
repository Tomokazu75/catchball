<%= form_with model: comment, url: post_comments_path(post) do |f| %>
  <%= f.text_area :text, placeholder: "コメント", class:"form-control" %>
  <%= f.submit "送信", class:"btn btn-primary mt-2" %>
<% end %>
<br>